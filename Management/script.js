// Fetch movies from the backend and display them on the webpage
function fetchMovies() {
    // Make an API call to the backend to get movie data
    // Update the DOM to display movies
}

// Call fetchMovies function when the page loads
document.addEventListener('DOMContentLoaded', fetchMovies);
