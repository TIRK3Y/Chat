const express = require('express');
const router = express.Router();

// Define your music route(s)
router.get('/', (req, res) => {
  // Handle logic for fetching and sending music data
});

module.exports = router;
