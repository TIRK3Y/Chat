const express = require('express');
const router = express.Router();

// Define your about route(s)
router.get('/', (req, res) => {
  // Handle logic for fetching and sending about data
});

module.exports = router;
